[project]
name = "heic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mohsenet", email = "mohsen.dbnet@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.3",
    "pillow>=12.3.0",
    "pillow-heif>=1.7.0",
]

[project.scripts]
heic = "heic:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
